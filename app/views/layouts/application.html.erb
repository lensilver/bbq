<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bbq" %></title>
    <link rel="stylesheet"
    href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css">
    
    <%= csrf_meta_tags %>

    <%= stylesheet_pack_tag "application", media: "all" %>

    <%= javascript_pack_tag "application" %>
  </head>
  
    <div class=" navbar navbar-expand-lg navbar-light bg-light navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li class="navbar-brand"><%= link_to t('app.bbq'), root_path %></li>
            <% if user_signed_in? %>
              <li class="navbar-brand"><%= link_to "Профиль", current_user %></li>
              <li class="navbar-brand"><%= link_to "Выйти", destroy_user_session_path, :method => :delete %></li>
            <% else %>
              <li class="navbar-brand"><%= link_to "Войти", new_user_session_path %></li>
              <li class="navbar-brand"><%= link_to "Зарегистрироваться", new_user_registration_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <div class="col-lg-9 mt-3">
          <% flash.each do |name, message| %>
          <div class="alert alert-dismissible fade show <%= bootstrap_class_for(name) %>">
            <%= message %>
            <button type="button" class="btn btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          </div>
          <% end %>
      
        <%= yield %>
        </div>

        <footer>
          <p>&copy; <%= t('app.company') %> 2022</p>
        </footer>
        
      </div>
    </div>

  
</html>